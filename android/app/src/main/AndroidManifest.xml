<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!--
      usesCleartextTraffic: the Host is plain ws:// by design (LAN-only, no TLS — see
      README's Security notes). Without this, targetSdk 28+ blocks cleartext sockets
      before they ever open, so every connection attempt fails silently pre-TCP and the
      app just loops on "Reconnecting" forever.
    -->
    <application
        android:name=".RemoteApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Remote"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Remote"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          connectedDevice fits this service's one job: keep a live WebSocket link to the
          paired laptop alive while the app is backgrounded. dataSync is meant for a
          finite upload/download, which doesn't describe an open bidirectional socket.
          Android 14 requires both the manifest type and the matching
          FOREGROUND_SERVICE_CONNECTED_DEVICE permission above, plus at least one
          companion connectivity permission from its allowed list — CHANGE_WIFI_STATE
          here, since the "connected device" is the laptop over WiFi/WebSocket, not
          bluetooth/NFC/USB.
        -->
        <service
            android:name=".service.RemoteConnectionService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

    </application>

</manifest>
